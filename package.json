{
  "name": "crowdedge",
  "version": "1.0.0",
  "description": "Autonomous prediction market agent on Monad",
  "main": "dist/agent.js",
  "scripts": {
    "build": "tsc",
    "agent": "node -r dotenv/config dist/agent.js",
    "dev": "ts-node -r dotenv/config src/agent.ts",
    "derive-address": "node -r dotenv/config -e \"const {privateKeyToAccount} = require('viem/accounts'); const a = privateKeyToAccount('0x' + process.env.PRIVATE_KEY); console.log('Wallet:', a.address)\"",
    "postinstall": "echo 'Dependencies installed via pnpm'"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "groq-sdk": "^0.7.0",
    "node-cron": "^3.0.3",
    "viem": "^2.17.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.2"
  }
}
